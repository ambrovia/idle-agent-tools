interface:
  display_name: "Pipeline"
  short_description: "Run work through pipeline-planner, pipeline-reviewer, and pipeline-builder personas"
  default_prompt: "Run this work package through the pipeline."
